# GY для бизнеса — прототип лендинга

Кликабельный прототип корпоративного направления Generation Yoga.
Структура «Вариант В»: распутье + 3 самостоятельные под-страницы.

## Как открыть

Просто откройте `index.html` в браузере — всё работает локально.
Для проверки форм и трекинга лучше запустить локальный сервер (иначе часть JS
браузер ограничивает на `file://`):

```
cd site
python -m http.server 8080
# затем открыть http://localhost:8080
```

## Страницы

| Файл          | Маршрут   | Аудитория                         |
|---------------|-----------|-----------------------------------|
| `index.html`  | `/`       | Распутье — выбор аудитории        |
| `hr.html`     | `/hr`     | HR: забота о команде              |
| `pr.html`     | `/pr`     | PR: событие для клиентов и гостей |
| `agency.html` | `/agency` | Ивент-агентства: модуль под ключ  |

Для рассылок дайте прямую ссылку на нужную под-страницу (агентствам — `/agency`).

## Что нужно заполнить команде (заглушки)

В вёрстке все незакрытые решения помечены **оранжевым пунктиром** (класс `.tbd`)
— их видно прямо на странице. Список:

- **Пробная практика** — минимальная цена «от … ₽» (hr.html, блок демо)
- **Примеры кейсов** в блоке «Задачи, которые решаем» — компания и детали каждого кейса после согласования (hr.html)

Именные кейсы согласованы и стоят в «Нам доверяют»: Райффайзен Банк и befree
(hr.html), ФК «Зенит», Yclients и ЦДС (pr.html); строка клиентов — в hero
agency.html. Годы намеренно не указываем.
- **Политика ПД** — реквизиты оператора и контакт для обращений (privacy.html); согласовать текст с юристом
- **PR-интеграция** — цена «от» (pr.html)
- **Комиссия агентствам** — % (agency.html)
- **White label** для агентств — да/нет (agency.html)
- **Сроки подготовки** — в FAQ на pr.html и agency.html
- **Ближайшая дата открытого события** — для пригласительного (agency.html)

Уже проставлено из документа правок: День заботы «от 65 000 ₽», Регулярность
«от 35 000 ₽/мес», цифры выгорания (71%, 2–6 окладов), FAQ для HR, демо-оффер,
сентябрьский дедлайн (до 15 августа).

> Цена «от» Дня заботы (65 000 ₽) ждёт сверки со сметой Маши. Правило: «от» —
> это цена, за которую реально можно купить. Дороже минимум — поднимаем цифру.

## Фото

Правило: **один кадр используется в одном месте** (иначе спорит с «1000+
мероприятий»). Отбор Маши лежит в `../vis-best/отбор`, остальные исходники — в
`../Visual`. Что где стоит:

| файл в `assets/img/` | место | исходник |
|---|---|---|
| `yoga-sunset-wide.jpg` | герой главной | набережная на закате |
| `office-circle.jpg` | главная, карточка «Забота о команде» | IMG_6110 |
| `lakhta-rooftop.jpg` | главная, карточка «Событие для гостей» | Лахта, терраса |
| `mass-practice-hall.jpg` | главная, карточка «Для агентств» | DSC03605 |
| `relax-closeup.jpg` | главная, полоса «Мировой тренд» | DSC_0679 |
| `yoga-embankment.jpg` | герой /hr | набережная |
| `courtyard-meditation.jpg` | /hr, «День заботы» | DSC_2037 |
| `hall-practice.jpg` | /hr, «Регулярность» | photo_2025-10-24 |
| `hang-drum.jpg` | /hr, «Антистресс и восстановление» | DSC_0428 |
| `light-show.jpg` | /hr, «Яркие впечатления» | 20250418-DSC02188-2 |
| `unity-circle.jpg` | /hr, «Объединение» | DSC04816 |
| `orangery-ecstatic.jpg` | герой /pr | DSC08029 (экстатик в Оранжерее) |
| `sup-yoga-water.jpg` | /pr, «SUP-йога на воде» | photo_2025-08-14 |
| `singing-bowl.jpg` | /pr, «Звукотерапия» | 20250418-DSC02152 |
| `orangery-practice.jpg` | /pr, «Оранжерея» | DSC_0044 |
| `planetarium-practice.jpg` | /pr, «Планетарий» | photo_2025-06-28 |
| `video-projection.jpg` | /pr, «Свечи и видеопроекция» | DSC_0266 |
| `beach-camp.jpg` | /pr, «Кемпы и йога-туры» | photo_2026-01-19 |
| `loft-performance.jpg` | герой /agency | 0E6A5368 |

### Галерея на главной

Блок «Как это выглядит» — 12 кадров плиткой 4:3 (компонент `.gallery`), задача
блока: показать разброс площадок и форматов, поэтому кадры намеренно разные —
крыша, огонь, набережная, оранжерея, планетарий, тур, шатёр, камерный круг:

`rooftop-group` (DSC_2042) · `fire-dome` (D03B3422) · `embankment-mass`
(photo_2026-06-28) · `evening-lounge` (photo 69) · `orangery-gong` (DSC_0647) ·
`bowls-sunlight` (photo_2026-06-27) · `sufi-dance` (photo 93) · `yoga-tour`
(Юлины туры) · `ballroom-yoga` (DSC_0365-2) · `planetarium-circle` (photo 56) ·
`festival-tent` (photo_2026-06-21) · `cozy-talk` (photo_2026-03-15).

Не используются (лежат про запас, можно удалить): `sound-bowls.jpg`,
`sound-hall.jpg`, `starry-dome.jpg` — кадры, которые заменил отбор Маши, и
`fire-show.jpg`, `rooftop-event.jpg`, `rooftop-sync.jpg`, `yoga-orangery.jpg` —
те же кадры, что ушли в галерею, только в старой нарезке 3:2.

Форматы под слоты: герой и шапка карточки — 3:2 (1600×1067), wow-плитка — 4:3,
полоса «Мировой тренд» — 2:1 (1600×800), плитка галереи — 4:3 (900×675),
качество JPEG 76–82.

Три кадра развилки на главной сведены по яркости и балансу белого: снимались
в разном свете (офис в пасмурный день, крыша на закате, зал с тёплым
прожектором) и рядом читались как случайный набор. Правка сделана в самих
файлах при экспорте — не CSS-фильтром: фильтр висел бы на каждой отрисовке и
всё равно не убрал бы разницу в экспозиции. Стандарт запрещает
«художественные вуали», а это выравнивание экспозиции, обычная допечатная
подготовка. Команды экспорта — в истории коммита, если понадобится повторить
для новых кадров.

### Ждём кадры (стоит плейсхолдер с брифом)

| место | что нужно снять |
|---|---|
| /hr, «Ретрит / Digital Detox» | выездной ретрит: практика на природе, малая группа (Маша запрашивает у Лены) |

Требование стандарта: в герое каждой страницы в кадре должны быть **люди на
практике**; затемнение — только придонным градиентом.

Из отбора остались неразобранными и годятся на замену: 0E6A5564 (неон,
музыканты — вторая «обложка для агентств»), photo 56 и photo 69 (планетарий),
Юлины йога-туры (photo_2026-07-08 ×2), D03B3484, DSC_0365-2, DSC_0647,
photo_2024-05-25 (сап на берегу), photo_2025-09-16 ×2, photo_2025-10-08,
photo_2026-03-15, photo_2026-06-21, photo_2_/photo_3_2026-08-12.

## Отслеживание / будущая CRM (заложено)

`assets/js/site.js` уже:

- ловит **UTM-метки и `id`** из URL (`?utm_source=…&id=agency_spb`), кладёт в
  `sessionStorage` и подставляет в скрытые поля форм;
- шлёт **события** в `window.dataLayer` и консоль: `page_view`, `cta_click`,
  `view_pricing` (скролл до цен), `form_submit`. Это заглушка — точки уже
  расставлены, останется подключить Метрику/CRM без переверстки.

## Заявки в Telegram

Кнопка «Связаться с менеджером» внизу главной открывает модальное окно
(нативный `<dialog>`, разметка в конце `index.html`). Отправка идёт не напрямую
в Telegram, а через воркер: токен бота нельзя класть в `site.js` — страница
статическая, токен увидели бы все.

Что сделать один раз, чтобы заявки пошли в чат:

1. Создать бота у @BotFather, забрать токен.
2. Написать боту, открыть `https://api.telegram.org/bot<ТОКЕН>/getUpdates`,
   взять `message.chat.id` (для группы id будет с минусом).
3. `cd worker && npx wrangler deploy`
4. `npx wrangler secret put BOT_TOKEN`, затем `npx wrangler secret put CHAT_ID`
5. Полученный адрес воркера вписать в `LEAD_ENDPOINT` в `assets/js/site.js`.

Пока `LEAD_ENDPOINT` пуст, форма ведёт себя как остальной прототип: показывает
экран «Заявка отправлена» с пометкой «демо-режим» и ничего не отправляет.
Как только адрес вписан, пометка со страницы уходит сама.

В форме уже стоят ловушка для ботов (скрытое поле `website` — заполненное
означает робота), обязательное согласие на обработку ПД и проброс UTM-меток.
`ALLOW_ORIGIN` в `worker/wrangler.toml` держать строго своим доменом, иначе
форму сможет дёргать чужой сайт.

Остальные формы прототипа (лид-магниты, заявки на под-страницах) пока живут в
демо-режиме — их можно перевести на тот же воркер, добавив им атрибут
`data-lead` вместо `data-stub`.

## Дизайн

Источник правды — `../gy-handoff/project/Визуальный стандарт GY.md` (эталон
вёрстки: `Главная - эталон.dc.html` там же). Кратко:

- Палитра: чернила `#0E0E0E`, бумага `#F6F1E8`, крем `#EFE7D6`, оранжевый
  бренда `#F26B1F`, второстепенный текст `#666`, волосяная линия
  `rgba(14,14,14,.12)`. Оранжевый — только акцент, никогда не заливка блока;
  один оранжевый фокус (кнопка) на экран.
- Шрифты: **Manrope 800 капсом** — все заголовки и кнопки; **Fraunces italic
  строчными** — одно-два акцентных слова в заголовке (разметка `<em>`).
  Подключены с Google Fonts — для офлайн/своего хостинга скачать локально.
- Сетка: контейнер 1160px, боковой padding 48px, вертикальный padding секции
  88px. Карточки белые, радиус 12, hairline-борд, min-height 260, фото-шапка
  170px, ссылка-действие прижата к низу.
- Ритм: база светлая, ink чередуется; **не более двух тёмных секций подряд**.
- Фото затемняются только придонным градиентом; цифры и подписи никогда не
  лежат поверх фото.

### Правило вёрстки

Вся вёрстка — в `assets/css/site.css`, всё поведение — в `assets/js/site.js`.
В страницах **не должно появляться ни `<style>`, ни `style="…"`**. Причина не
в эстетике: у инлайн-стиля специфичность выше любого селектора, поэтому он молча
перебивает и медиазапросы (адаптив ломается без единой ошибки в консоли), и
компонентные правила. Нужен разовый стиль — заводите класс:

- страничные компоненты (`.builder`, `.catalog`, `.terms`, `.wow`, `.policy`) —
  в секции «Страничные блоки» в конце `site.css`, обязательно со скоупом по
  классу страницы, иначе правило утечёт на соседние страницы;
- разовые отступы и кегли — в секции «Утилиты» (`.p-lg`, `.p-sm`, `.lede--wide`,
  `.form-head`, `.cta-row--split`, `.section-head--tight`).

Утилиты стоят **после** компонентов и у текстовых в селекторе есть элемент
(`p.p-sm`) — иначе `.p-sm` проигрывает `.card p` по специфичности и не работает.

### Один файл или несколько — по ситуации

Сейчас весь CSS в одном `site.css`, и это осознанный выбор под текущий размер, а
не догма. Пять страниц, страничных стилей ~70 строк, посетитель ходит
распутье → раздел → форма — при одном файле браузер скачивает CSS ровно раз и
дальше берёт из кэша. Плюс токены и компоненты лежат вместе, страничному файлу
негде разъехаться со стандартом.

**Разделить на `assets/css/pages/*.css` стоит, когда** появится хотя бы одно из:

- страниц становится заметно больше 5–7, и общий файл превращается в свалку;
- у отдельной страницы набирается своих 150–200+ строк (лендинг под кампанию,
  калькулятор, личный кабинет) — тогда остальным страницам незачем её возить;
- страницы начинают жить разными релизами или их правят разные люди — отдельный
  файл убирает конфликты в общем;
- появляется сборка, которая сама режет CSS по страницам, — тогда вопрос
  решается за нас.

Схема при разделении: `site.css` — токены, типографика, компоненты, утилиты
(подключается везде); `pages/hr.css` и т.д. — только то, что живёт на одной
странице, подключается вторым `<link>` после общего. На HTTP/2 (а Pages отдаёт
именно по нему) второй запрос стоит дёшево, так что дробить не страшно.

Чего делать не стоит в обоих случаях: возвращать `<style>` и `style=` в
разметку и тащить сборщик (PostCSS, Sass, бандлер) ради статики — Pages отдаёт
файлы как есть, а шаг сборки добавит звено, которое нечем окупить.
